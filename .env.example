# Provider credentials consumed by browser-use (choose the provider you configure)
OPENAI_API_KEY=
CUSTOM_LLM_API_KEY=
# 固定 Playwright 登录模式使用；不要把真实账号密码写入 YAML。
TEST_USERNAME=
TEST_PASSWORD=
# Configuration paths may also be supplied to the CLI options.
